name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # 🔥 Every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Health Endpoint
        run: |
          curl -m 10 https://agentic-honeypot-api.onrender.com/health || true
          curl -m 10 https://agentic-honeypot-api.onrender.com/analyze || true

